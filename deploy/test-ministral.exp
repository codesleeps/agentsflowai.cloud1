#!/usr/bin/expect -f
set timeout 60
set password "n/6Y#PRl(.ovLy@D,veW"
spawn ssh root@72.61.16.111
expect "password:"
send "$password\r"
expect "# "

send "ollama run ministral-3:3b 'Say hello!'\r"
expect {
    "Hello" {
        puts "Ministral works!"
    }
    "Error" {
        puts "Ministral failed."
    }
}
expect "# "
send "exit\r"
expect eof
