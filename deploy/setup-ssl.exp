#!/usr/bin/expect -f
set timeout 300
set password "n/6Y#PRl(.ovLy@D,veW"
spawn ssh root@72.61.16.111
expect "password:"
send "$password\r"
expect "# "
send "certbot --nginx -d agentsflowai.cloud -d www.agentsflowai.cloud --non-interactive --agree-tos -m admin@agentsflowai.cloud\r"
expect "# "
send "systemctl reload nginx\r"
expect "# "
send "exit\r"
interact
